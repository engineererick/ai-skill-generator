import type { TemplateConfig } from './frontend-config.js';

export const basicConfig: TemplateConfig = {
  name: 'Basic',
  description: 'Minimal skill with only SKILL.md',
  questions: [],
  variables: {},
};
